services:
  ai-postgres:
    image: postgres:16
    container_name: ai-postgres
    ports:
      - 5434:5432
    environment:
      POSTGRES_DB: ai_db
      POSTGRES_USER: ai_user
      POSTGRES_PASSWORD: ai_pass
    volumes:
      - ai-pg-data:/var/lib/postgresql/data

volumes:
  ai-pg-data: